import { WindowsWidthDirective } from './windows-width.directive';

describe('WindowsWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new WindowsWidthDirective();
    expect(directive).toBeTruthy();
  });
});
