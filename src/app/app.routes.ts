import { Routes } from '@angular/router';
import { CreateChannelComponent } from './features/create-channel/create-channel.component';


export const routes: Routes = [
    { path: '', component: CreateChannelComponent },
];
