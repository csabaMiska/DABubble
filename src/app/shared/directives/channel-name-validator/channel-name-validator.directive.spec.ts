import { ChannelNameValidatorDirective } from './channel-name-validator.directive';

describe('ChannelNameValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ChannelNameValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
